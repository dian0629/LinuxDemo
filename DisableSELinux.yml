---
- name: DISABLE SELINUX
  hosts: all
  become: yes
  
  tasks:
    - name: disable selinux
      selinux:
        state: disabled
      register: selinux
      
    - name: show status
      debug:
        var: selinux.state
